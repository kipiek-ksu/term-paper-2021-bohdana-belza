      program p1;
                  var r,x,y:real;
                  begin
                  read(x,y);
                  r:=(abs(x)-abs(y))/(1+abs(x*y);
                  write(r:20:3);
                  end.